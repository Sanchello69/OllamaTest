# Чеклист запуска приложения

## Перед началом работы

### ✅ Требования системы

- [ ] Java 17 или выше установлена
  ```bash
  java -version
  # Должна быть версия 17+
  ```

- [ ] Ollama установлен и запущен
  ```bash
  ollama serve
  # В отдельном терминале
  ```

- [ ] Модель эмбеддингов загружена
  ```bash
  ollama pull nomic-embed-text
  ollama list
  # Должна быть в списке
  ```

## Сборка проекта

### Вариант A: IntelliJ IDEA (рекомендуется)

- [ ] Открыть проект в IntelliJ IDEA
- [ ] Дождаться загрузки зависимостей Gradle
- [ ] Проверить, что нет ошибок компиляции
- [ ] Build → Build Project

### Вариант B: Командная строка

- [ ] Установить Gradle (если еще нет)
- [ ] Выполнить:
  ```bash
  cd OllamaTest
  gradle wrapper
  ./gradlew build
  ```

### Вариант C: Docker

- [ ] Установить Docker
- [ ] Выполнить:
  ```bash
  docker build -t ollama-embeddings .
  docker-compose up -d
  ```

## Тестирование

### Шаг 1: Проверка Ollama

```bash
curl http://localhost:11434/api/tags
```

**Ожидаемый результат**: JSON с моделями

- [ ] Ollama отвечает
- [ ] В списке есть `nomic-embed-text`

### Шаг 2: Индексация примера

```bash
# Gradle
./gradlew run --args="index example.rtf"

# Или JAR
java -jar build/libs/OllamaTest-1.0-SNAPSHOT.jar index example.rtf

# Или Docker
docker run --rm --network host -v $(pwd):/app/data ollama-embeddings index /app/data/example.rtf
```

**Ожидаемый результат**:
```
📄 Processing RTF file: example.rtf
Step 1: Parsing RTF file...
✓ Extracted ~3200 characters
Step 2: Splitting text into chunks...
✓ Created 8 chunks
Step 3: Generating embeddings with Ollama...
Progress: 8/8 chunks processed
✓ Generated 8 embeddings
...
✅ Processing complete!
```

- [ ] RTF файл успешно обработан
- [ ] Создано ~8 чанков
- [ ] Сгенерированы эмбеддинги
- [ ] Создан файл `embeddings_index.json`

### Шаг 3: Проверка индекса

```bash
ls -lh embeddings_index.json
```

- [ ] Файл существует
- [ ] Размер ~15-20 KB

```bash
# Просмотр статистики
./gradlew run --args="stats embeddings_index.json"
# Или
java -jar build/libs/OllamaTest-1.0-SNAPSHOT.jar stats embeddings_index.json
```

**Ожидаемый результат**:
```
Index Statistics:
  Total entries: 8
  Dimension: 384
  Index size: ~0 MB
```

- [ ] Статистика отображается корректно
- [ ] Total entries: 8
- [ ] Dimension: 384

### Шаг 4: Поиск

```bash
./gradlew run --args="search embeddings_index.json 'machine learning applications'"
# Или
java -jar build/libs/OllamaTest-1.0-SNAPSHOT.jar search embeddings_index.json "machine learning"
```

**Ожидаемый результат**:
```
🔍 Searching index: embeddings_index.json
Query: "machine learning applications"
...
Results:

1. Score: 0.8xxx
   Text: Machine learning has numerous applications...
   Metadata: {chunk_index=3, source_file=example.rtf}

2. Score: 0.7xxx
   ...
```

- [ ] Поиск работает
- [ ] Возвращены релевантные результаты
- [ ] Score в диапазоне 0.0-1.0
- [ ] Текст соответствует запросу

## Дополнительные проверки

### Проверка 5: Работа с разными запросами

```bash
# Тест 1: Deep Learning
./gradlew run --args="search embeddings_index.json 'deep learning neural networks'"

# Тест 2: Supervised Learning
./gradlew run --args="search embeddings_index.json 'supervised learning'"

# Тест 3: Applications
./gradlew run --args="search embeddings_index.json 'healthcare finance applications'"
```

- [ ] Все запросы возвращают релевантные результаты
- [ ] Score разумные (> 0.5 для релевантных результатов)

### Проверка 6: Настройка параметров чанкинга

```bash
# Большие чанки
./gradlew run --args="index example.rtf index_large.json 1000 100"

# Малые чанки
./gradlew run --args="index example.rtf index_small.json 200 20"
```

- [ ] Индексы создаются с разными параметрами
- [ ] Количество чанков отличается

### Проверка 7: Работа с собственным RTF файлом

```bash
# Создайте свой RTF файл или используйте существующий
./gradlew run --args="index /path/to/your/file.rtf my_index.json"
./gradlew run --args="search my_index.json 'ваш запрос'"
```

- [ ] Собственный файл обрабатывается
- [ ] Поиск работает

## Устранение проблем

### Проблема: "Connection refused" при обращении к Ollama

**Решение**:
```bash
# Проверить статус
curl http://localhost:11434/api/tags

# Запустить Ollama
ollama serve
```

- [ ] Ollama запущен
- [ ] Доступен по http://localhost:11434

### Проблема: "Model not found"

**Решение**:
```bash
ollama pull nomic-embed-text
ollama list
```

- [ ] Модель загружена
- [ ] Видна в списке

### Проблема: Медленная генерация эмбеддингов

**Ожидаемое время**: 0.1-0.5s на чанк

**Если медленнее**:
- [ ] Проверить загрузку CPU
- [ ] Проверить память
- [ ] Дождаться загрузки модели (первый запрос ~30s)

### Проблема: BUILD FAILED

**Решение**:
```bash
./gradlew clean
rm -rf .gradle build
./gradlew build
```

- [ ] Проект пересобран
- [ ] Нет ошибок компиляции

### Проблема: OutOfMemoryError

**Решение**:
```bash
java -Xmx4G -jar build/libs/OllamaTest-1.0-SNAPSHOT.jar index file.rtf
```

Или уменьшить размер чанков:
```bash
./gradlew run --args="index file.rtf index.json 200 20"
```

- [ ] Память увеличена
- [ ] Или чанки уменьшены

## Финальная проверка

### Все основные функции работают

- [ ] ✅ RTF парсинг
- [ ] ✅ Разбиение на чанки
- [ ] ✅ Генерация эмбеддингов через Ollama
- [ ] ✅ Сохранение индекса
- [ ] ✅ Загрузка индекса
- [ ] ✅ Семантический поиск
- [ ] ✅ Статистика индекса

### Производительность приемлема

- [ ] Парсинг RTF < 1s
- [ ] Чанкинг < 0.1s
- [ ] Генерация эмбеддингов ~0.1-0.5s на чанк
- [ ] Поиск < 0.1s

### Качество результатов

- [ ] Поисковые результаты релевантны
- [ ] Score коррелирует с релевантностью
- [ ] Метаданные корректны

## Опциональные проверки (расширенные)

### Docker проверки

```bash
docker build -t ollama-embeddings .
docker run --rm --network host -v $(pwd):/app/data ollama-embeddings index /app/data/example.rtf
```

- [ ] Docker образ собран
- [ ] Приложение работает в контейнере

### docker-compose проверки

```bash
docker-compose up -d
docker-compose ps
docker-compose logs ollama
```

- [ ] Ollama запущен в Docker
- [ ] Модель загружена
- [ ] Healthcheck проходит

## Готово к использованию!

Если все чекбоксы отмечены ✅, приложение готово к работе!

### Следующие шаги

1. Прочитать полную документацию в README.md
2. Изучить примеры в QUICKSTART.md
3. Экспериментировать с параметрами
4. Интегрировать в свои проекты

## Быстрая справка

```bash
# Индексация
./gradlew run --args="index file.rtf"

# Поиск
./gradlew run --args="search embeddings_index.json 'query'"

# Статистика
./gradlew run --args="stats embeddings_index.json"

# Помощь
./gradlew run --args="--help"
```

---

**Если что-то не работает**:
1. Проверьте требования системы
2. Прочитайте раздел "Устранение неполадок" в README.md
3. Проверьте логи ошибок
4. Убедитесь, что Ollama запущен и модель загружена
